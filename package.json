{
  "name": "flow-demo",
  "main": "lib/index.js",
  "version": "1.0.0",
  "description": "this is a demo with flow static checker",
  "scripts": {
    "build": "babel src/ -d lib/",
    "flow": "flow",
    "prepublish": "yarn run build",
    "compile": "./node_modules/.bin/babel src/ -d lib/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "directories": {
    "lib": "lib"
  },
  "dependencies": {},
  "devDependencies": {
    "flow-bin": "^0.202.0",
    "@babel/cli": "^7.21.0",
    "@babel/core": "^7.21.3",
    "@babel/preset-flow": "^7.18.6"
  },
  "author": "Zowie",
  "license": "ISC"
}
